#include <stdio.h>
#include <math.h>

int main() {
     int num;
    printf("Enter Number You Need squre :- ");
    scanf("%d", &num);
    printf("Square root of %d = %.0f\n",num, sqrt(num));
    return 0;
}